@model IEnumerable<PracticaFinal.Models.CuotaInversion>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FechaPlanificada)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Monto)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FechaRealizada)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CodComprobante)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdInversionNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TipoTransaccionNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FechaPlanificada)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Monto)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FechaRealizada)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CodComprobante)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdInversionNavigation.IdInversion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TipoTransaccionNavigation.IdTipo)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CodCuota">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CodCuota">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CodCuota">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
